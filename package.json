{
  "name": "@codemastersolutions/commitzero",
  "version": "0.0.49",
  "description": "Conventional Commits validator with a friendly CLI, Git hooks, and an internal rule engine — zero runtime dependencies.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=16"
  },
  "homepage": "https://github.com/codemastersolutions/commitzero#readme",
  "bugs": {
    "url": "https://github.com/codemastersolutions/commitzero/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/codemastersolutions/commitzero.git"
  },
  "keywords": [
    "conventional commits",
    "conventional",
    "commits",
    "commitizen",
    "commitlint",
    "git hooks",
    "cli",
    "codemastersolutions",
    "codemaster",
    "commitzero",
    "code",
    "codemaster solutions",
    "codemaster solucoes",
    "solucoes"
  ],
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "build:esm": "tsc -p tsconfig.esm.json",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build": "pnpm run build:esm && pnpm run build:cjs && node scripts/postbuild-cjs.js",
    "clean": "rm -rf dist",
    "pretest": "pnpm run build",
    "test": "node --import tsx --test test/unit/select.test.ts test/unit/formatter.test.ts test/unit/rules.test.ts test/unit/parser.test.ts test/integration/cleanup.test.ts test/integration/cli.test.ts test/integration/precommit.test.ts && COMMITSKIP_SELECT_PROMPT=1 node --import tsx --test test/unit/commit.test.ts",
    "coverage": "pnpm run build && COMMITSKIP_ADD_PROMPT=1 c8 -r text-summary -r lcov node --import tsx --test test/unit/select.test.ts test/unit/formatter.test.ts test/unit/rules.test.ts test/unit/parser.test.ts test/unit/commit.test.ts test/integration/cleanup.test.ts test/integration/cli.test.ts test/integration/precommit.test.ts",
    "coverage:subset": "pnpm run build && c8 -r text-summary -r lcov node --import tsx --test test/unit/select.test.ts test/unit/formatter.test.ts test/integration/cleanup.test.ts test/integration/cli.test.ts test/integration/precommit.test.ts",
    "verify:readmes": "node scripts/verify-readmes.js",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,js,json,md}\"",
    "comments:strip": "node scripts/strip-comments.js && pnpm run format",
    "simulate:release": "node scripts/simulate-release.js",
    "sonar:up": "docker compose -f docker-compose.sonarqube.yml up -d",
    "sonar:down": "docker compose -f docker-compose.sonarqube.yml down -v",
    "sonar:auth:check": "node --input-type=module -e \"const host=process.env.SONAR_HOST_URL || 'http://localhost:9000'; const token=process.env.SONAR_TOKEN; const login=process.env.SONAR_LOGIN; const password=process.env.SONAR_PASSWORD; const url=new URL('/api/authentication/validate', host); const headers={}; if(token){ headers.Authorization='Basic ' + Buffer.from(token + ':').toString('base64'); console.log('Auth: token (len=' + token.length + ')'); } else if(login && password){ headers.Authorization='Basic ' + Buffer.from(login + ':' + password).toString('base64'); console.log('Auth: login/password'); } else { console.error('Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD'); process.exit(1); } const res=await fetch(url,{headers}); const body=await res.text(); console.log('HTTP ' + res.status); console.log(body);\"",
    "sonar:scan": "node --input-type=module -e \"const host=process.env.SONAR_HOST_URL || 'http://localhost:9000'; const token=process.env.SONAR_TOKEN; const login=process.env.SONAR_LOGIN; const password=process.env.SONAR_PASSWORD; console.log('Using SONAR_HOST_URL=' + host); const url=new URL('/api/authentication/validate', host); const headers={}; if(token){ headers.Authorization='Basic ' + Buffer.from(token + ':').toString('base64'); console.log('Auth: token (len=' + token.length + ')'); } else if(login && password){ headers.Authorization='Basic ' + Buffer.from(login + ':' + password).toString('base64'); console.log('Auth: login/password'); } else { console.error('Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD'); process.exit(1); } const res=await fetch(url,{headers}); if(!res.ok){ console.error('Auth check failed: HTTP ' + res.status); process.exit(1); } const json=await res.json(); if(!json.valid){ console.error('Auth invalid (token/login not accepted by SonarQube)'); process.exit(1); } console.log('Auth valid');\" && pnpm run coverage && (SCANNER_URL=\"${SONAR_SCANNER_HOST_URL:-http://host.docker.internal:9000}\"; if [ -n \"$SONAR_TOKEN\" ]; then AUTH_ARGS=\"-Dsonar.token=$SONAR_TOKEN\"; elif [ -n \"$SONAR_LOGIN\" ] && [ -n \"$SONAR_PASSWORD\" ]; then AUTH_ARGS=\"-Dsonar.login=$SONAR_LOGIN -Dsonar.password=$SONAR_PASSWORD\"; else echo \"Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD\"; exit 1; fi; docker run --rm --add-host=host.docker.internal:host-gateway -e SONAR_USER_HOME=/tmp/.sonar -v \"$PWD:/usr/src\" sonarsource/sonar-scanner-cli:latest -Dsonar.host.url=\"$SCANNER_URL\" $AUTH_ARGS)",
    "sonar:export:metrics": "node --input-type=module -e \"import fs from 'node:fs'; import path from 'node:path'; const host=process.env.SONAR_HOST_URL || 'http://localhost:9000'; const token=process.env.SONAR_TOKEN; const login=process.env.SONAR_LOGIN; const password=process.env.SONAR_PASSWORD; let auth=''; if(token){ auth='Basic ' + Buffer.from(token + ':').toString('base64'); } else if(login && password){ auth='Basic ' + Buffer.from(login + ':' + password).toString('base64'); } else { console.error('Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD'); process.exit(1); } const dir=path.resolve('sonar'); fs.mkdirSync(dir,{recursive:true}); const url=new URL('/api/measures/component', host); url.searchParams.set('component','commitzero'); url.searchParams.set('metricKeys','bugs,vulnerabilities,security_hotspots,code_smells,coverage,duplicated_lines_density,ncloc'); const res=await fetch(url,{headers:{Authorization:auth}}); if(!res.ok){throw new Error('HTTP ' + res.status + ' ' + res.statusText);} const json=await res.json(); fs.writeFileSync(path.join(dir,'metrics.json'), JSON.stringify(json,null,2)+'\\\\n'); console.log('Wrote sonar/metrics.json');\"",
    "sonar:export:quality-gate": "node --input-type=module -e \"import fs from 'node:fs'; import path from 'node:path'; const host=process.env.SONAR_HOST_URL || 'http://localhost:9000'; const token=process.env.SONAR_TOKEN; const login=process.env.SONAR_LOGIN; const password=process.env.SONAR_PASSWORD; let auth=''; if(token){ auth='Basic ' + Buffer.from(token + ':').toString('base64'); } else if(login && password){ auth='Basic ' + Buffer.from(login + ':' + password).toString('base64'); } else { console.error('Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD'); process.exit(1); } const dir=path.resolve('sonar'); fs.mkdirSync(dir,{recursive:true}); const url=new URL('/api/qualitygates/project_status', host); url.searchParams.set('projectKey','commitzero'); const res=await fetch(url,{headers:{Authorization:auth}}); if(!res.ok){throw new Error('HTTP ' + res.status + ' ' + res.statusText);} const json=await res.json(); fs.writeFileSync(path.join(dir,'quality-gate.json'), JSON.stringify(json,null,2)+'\\\\n'); console.log('Wrote sonar/quality-gate.json');\"",
    "sonar:export:issues": "node --input-type=module -e \"import fs from 'node:fs'; import path from 'node:path'; const host=process.env.SONAR_HOST_URL || 'http://localhost:9000'; const token=process.env.SONAR_TOKEN; const login=process.env.SONAR_LOGIN; const password=process.env.SONAR_PASSWORD; let auth=''; if(token){ auth='Basic ' + Buffer.from(token + ':').toString('base64'); } else if(login && password){ auth='Basic ' + Buffer.from(login + ':' + password).toString('base64'); } else { console.error('Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD'); process.exit(1); } const dir=path.resolve('sonar'); fs.mkdirSync(dir,{recursive:true}); const pageSize=500; let page=1; const issues=[]; while(true){ const url=new URL('/api/issues/search', host); url.searchParams.set('componentKeys','commitzero'); url.searchParams.set('statuses','OPEN,REOPENED,CONFIRMED'); url.searchParams.set('ps', String(pageSize)); url.searchParams.set('p', String(page)); const res=await fetch(url,{headers:{Authorization:auth}}); if(!res.ok){throw new Error('HTTP ' + res.status + ' ' + res.statusText);} const json=await res.json(); issues.push(...(json.issues||[])); const total=Number(json.total||0); if(page*pageSize>=total) break; page++; } fs.writeFileSync(path.join(dir,'issues.json'), JSON.stringify({projectKey:'commitzero', exportedAt:new Date().toISOString(), issues},null,2)+'\\\\n'); console.log('Wrote sonar/issues.json (' + issues.length + ' issues)');\"",
    "sonar:export:hotspots": "node --input-type=module -e \"import fs from 'node:fs'; import path from 'node:path'; const host=process.env.SONAR_HOST_URL || 'http://localhost:9000'; const token=process.env.SONAR_TOKEN; const login=process.env.SONAR_LOGIN; const password=process.env.SONAR_PASSWORD; let auth=''; if(token){ auth='Basic ' + Buffer.from(token + ':').toString('base64'); } else if(login && password){ auth='Basic ' + Buffer.from(login + ':' + password).toString('base64'); } else { console.error('Provide SONAR_TOKEN or SONAR_LOGIN and SONAR_PASSWORD'); process.exit(1); } const dir=path.resolve('sonar'); fs.mkdirSync(dir,{recursive:true}); const pageSize=500; let page=1; const hotspots=[]; while(true){ const url=new URL('/api/hotspots/search', host); url.searchParams.set('projectKey','commitzero'); url.searchParams.set('ps', String(pageSize)); url.searchParams.set('p', String(page)); const res=await fetch(url,{headers:{Authorization:auth}}); if(!res.ok){throw new Error('HTTP ' + res.status + ' ' + res.statusText);} const json=await res.json(); hotspots.push(...(json.hotspots||[])); const paging=json.paging||{}; const total=Number(paging.total||0); if(page*pageSize>=total) break; page++; } fs.writeFileSync(path.join(dir,'hotspots.json'), JSON.stringify({projectKey:'commitzero', exportedAt:new Date().toISOString(), hotspots},null,2)+'\\\\n'); console.log('Wrote sonar/hotspots.json (' + hotspots.length + ' hotspots)');\"",
    "sonar:export": "pnpm sonar:export:metrics && pnpm sonar:export:quality-gate && pnpm sonar:export:issues && pnpm sonar:export:hotspots",
    "postinstall": "node ./scripts/postinstall-shim.js",
    "preuninstall": "node ./scripts/cleanup-shim.js || true",
    "uninstall": "node ./scripts/cleanup-shim.js || true",
    "postuninstall": "node ./scripts/cleanup-shim.js || true",
    "commitzero": "commitzero",
    "commitzero:install": "pnpm run commitzero install-hooks",
    "commitzero:uninstall": "pnpm run commitzero uninstall-hooks",
    "commit": "pnpm run commitzero commit",
    "commit:push": "pnpm run commitzero commit --add --push"
  },
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "bin": {
    "commitzero": "./dist/esm/cli/index.js"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "files": [
    "dist/",
    "scripts/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "devDependencies": {
    "@codemastersolutions/commitzero": "^0.0.49",
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.12.2",
    "c8": "^11.0.0",
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "fast-glob": "^3.3.3",
    "prettier": "^3.6.2",
    "strip-comments": "^2.0.1",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.46.0"
  }
}
